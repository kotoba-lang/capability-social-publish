# capability-social-publish

Atomic authority package for `social/publish`.

Bootstrap commit. Contents are generated by
`kotoba-lang/kotoba-core-contracts`'s `scaffold-capability-repos` once the
`social/publish` entry has landed in the core contract; this commit exists so
the repository has a Radicle identity to pin, since the contract requires a
`:capability/radicle-rid` before its catalog validates.

- capability id: `social/publish`
- imports: `#{:social-publish :social-publish-receipt}`
- effects: `#{:network-write :data-egress :external-communication}`
- default policy: `:approval-required`

Consumed by `kotoba-lang/haishin` (配信), the cross-platform syndication plane.
